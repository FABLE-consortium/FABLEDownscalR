% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/id_infra.R
\name{fdr_make_id_raster}
\alias{fdr_make_id_raster}
\title{Build an ID raster (each raster pixel = ns_int)}
\usage{
fdr_make_id_raster(
  grid_sf,
  ns_map,
  id_col = "id_c",
  crs_equal_area = 6933,
  pixel_res_m = .fdr_defaults$pixel_res_m,
  touches = TRUE
)
}
\arguments{
\item{grid_sf}{sf polygon object with an id column (default "id_c")}

\item{ns_map}{tibble/data.frame containing id_c and ns_int}

\item{id_col}{name of the id column in grid_sf and ns_map (default "id_c")}

\item{crs_equal_area}{EPSG code for equal area calculations (default 6933)}

\item{pixel_res_m}{pixel resolution in meters (e.g. 50000, 10000, 1000)}

\item{touches}{terra rasterize touches argument (default TRUE; helps avoid missing thin polygons)}
}
\value{
A SpatRaster with values = ns_int
}
\description{
Why:
\itemize{
\item ggplot needs a dense pixel canvas (x,y grid) to plot values.
\item We rasterize the grid polygons so each pixel stores the cell ID (ns_int).
\item Later, we can join any model outputs by ns_int and paint maps quickly.
}
}
\details{
Inputs:
\itemize{
\item grid geometry: sf polygons with id_c
\item ns_map: id_c -> ns_int
}
}
